foreach(subdir IN ITEMS config_catalog runtime router local_models cloud_bridge telemetry openai_compat)
    if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/${subdir}/CMakeLists.txt")
        add_subdirectory(${subdir})
    endif()
endforeach()
