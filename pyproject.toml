[tool.uv.workspace]
members = [
    "seceda_cloud",
]

[dependency-groups]
dev = [
    "pytest",
    "ruff",
]

[tool.ruff]
line-length = 100

[tool.pytest.ini_options]
testpaths = [
    "seceda_cloud/tests",
]
