[tool.uv.workspace]
members = [
    "seceda_cloud",
    "seceda_edge",
]

[dependency-groups]
dev = [
    "pytest",
    "ruff",
]

[tool.ruff]
line-length = 100

[tool.pytest.ini_options]
testpaths = [
    "seceda_cloud/tests",
    "seceda_edge/tests",
]
