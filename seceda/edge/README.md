# seceda-edge

This area contains optional edge-side Python helpers, experiments, and tooling for Seceda.

Use the repo-root `pyproject.toml` with `uv sync --extra edge`, then run
commands with `uv run --extra edge ...`.
